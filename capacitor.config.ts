import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 't02e01Recetario',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
